/// 2. Understand the reason behind directly using datum producing in type signature input is unpopular in Cardano development 
///    - since it can be accessed from context.transaction
/// 4. Can distinguish between: the smart contract updates the datum vs the script validate if datum is updated -> building early concept on cardano validator
